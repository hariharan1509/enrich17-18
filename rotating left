#include<stdio.h>
#include<string.h>
int main()
{
 char s[100};
 int a,l;
 scanf("%s %d",s,&a);
 l=strlen(s);
 for(int i=a;i<l;i++)
   printf("%c",s[i]);
 for(int i=0;i<a;i++)
   printf("%c",s[i];
 return 0;
 }
 
